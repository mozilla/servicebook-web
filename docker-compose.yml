version: '2'
services:
    servicebook:
        image: servicebook/dev
    web:
        build: .
        ports:
        - "5000:5000"
        links:
        - servicebook
        volumes:
        - .:/code    
